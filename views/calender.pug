extends layout
block content

    each quote in qoutes
        li.qoute_item: label(for=quote.param)= getDate(, quote.id)
        p= quote.id
        p= quote.quote
        p= quote.book
        p= quote.chapter
    
